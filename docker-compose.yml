services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: kds_user
      POSTGRES_PASSWORD: kds_pass
      POSTGRES_DB: kds_db
